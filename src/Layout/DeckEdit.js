import React from "react";
import { readDeck, readCard } from "../utils/api";

// readDeck() loads deck that contains the card to be edited
// readCard() loads the card you want to edit

// if user clicks on either "save" or "cancel", the user is taken to the CurrentDeck screen

export default function DeckEdit() {
    return (

    );
};